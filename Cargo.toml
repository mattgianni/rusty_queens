[package]
name = "rusty_queens"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
clap = { version = "4.5.7", features = ["derive"] }
cfg-if = "1.0"

[build-dependencies]
vergen = {version = "8.3.1", features = ["build", "git", "gitcl", "rustc"] }
anyhow = "1.0"